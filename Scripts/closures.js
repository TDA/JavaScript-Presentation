/**
 * Created by schandramouli on 4/7/16.
 */

var names = ['zero', 'one', 'two'],
  i;

// simple
var introductions = [];
for (i = 0; i < 3; i++) {
  introductions[i] = names[i];
}
console.log(introductions); // ?
